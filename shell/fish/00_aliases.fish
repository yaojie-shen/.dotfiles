# Fish counterpart of shell/common/00_aliases.sh.

alias net='speedometer -r eth0 -t eth0'

alias nvidia-smi-watch='watch -n 1 nvidia-smi'

alias pipv="pip3 --version"

# SSH Config Manager
alias sshm="ssh_manager"
