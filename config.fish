# Only source default config from ~/.shell/source.fish, same layout as zshrc/bashrc.
# Machine-specific additions belong in ~/.persistent/custom.fish or
# ~/.persistent/custom/shell/*.fish, so this file stays conflict-free across machines.
source "$HOME/.shell/source.fish"
